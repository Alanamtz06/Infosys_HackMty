fastapi==0.115.0
uvicorn[standard]==0.30.6
osmnx==1.9.4
networkx==3.3
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
python-dotenv==1.0.1
websockets==13.1
numpy==1.26.4
pandas==2.2.3
google-generativeai==0.8.3
bcrypt==4.2.0
scikit-learn==1.9.1

# Batching (decision/vrptw_solver.py). Pinned a esta version especificamente
# porque >=9.10 exige protobuf>=6.33 y numpy>=2.0, lo que rompe osmnx==1.9.4
# (numpy<1.27) y google-generativeai==0.8.3 (protobuf<6) de arriba.
ortools==9.9.3963

# Estado de turnos compartido entre workers (api/routes/session_store.py).
# Solo se usa si REDIS_URL esta en el .env; sin eso el backend corre con
# estado en memoria y no necesita un servidor de Redis.
redis==8.1.0

# tests (backend/tests/, ver backend/pytest.ini)
pytest==9.1.1
fakeredis==2.38.0
